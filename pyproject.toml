[project]
name = "ml-pipeline-evaluation"
version = "0.1.0"
description = "Clean scikit-learn pipeline + experiment logging + metrics (reproducible)."
requires-python = ">=3.10"
authors = [{name="Ragouguelaba Agoda Koussema"}]

[tool.ruff]
line-length = 100
target-version = "py310"
